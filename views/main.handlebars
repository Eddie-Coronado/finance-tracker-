<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Finance Tracker</title>
  <link rel="stylesheet" type="text/css" href="./css/style.css">
</head>

<body>
  <header>
    <h1>Welcome to a better way to track your finances!</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    {{#if loggedIn}}
    <button id="logout" class="Nate is working on CSS">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>
  <main>
    <aside>
      <h2>Enjoy!</h2>
    </aside>
    <div class="Nate is working on CSS">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="./js/logout.js"></script>
  {{/if}}
</body>

</html>